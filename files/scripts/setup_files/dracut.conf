# Dracut configuration for container build environment
# Disable syslog logging to prevent /dev/log socket issues
stdloglvl=4
sysloglvl=0
kmsgloglvl=0
fileloglvl=0
# Skip initramfs generation during kernel install - will be done later
noimageifnotneeded="yes"