name: Deploy Images to GHCR

on:
  push:
    branches:
      - gaming-images
  schedule:
    - cron: '0 6 * * 6'
  workflow_dispatch:

# Required permissions for signing with Cosign
permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - image: cosmic-atomic
            desktop: cosmic
          - image: silverblue
            desktop: gnome
          - image: kinoite
            desktop: kde
    defaults:
      run:
        working-directory: '.'
    steps:
      - name: 'Checkout GitHub Action'
        uses: actions/checkout@main

      - name: 'Install Cosign'
        uses: sigstore/cosign-installer@v3.8.2

      - name: 'Delete huge unnecessary tools folder'
        run: rm -rf /opt/hostedtoolcache

      - name: 'Login to GitHub Container Registry'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 'Build and Push Image'
        run: |
          podman build \
            --build-arg BASE_IMAGE_NAME=${{ matrix.image }} \
            --build-arg DESKTOP_ENV=${{ matrix.desktop }} \
            --tag ghcr.io/m-fe02/hackpod-os:${{ matrix.image }}-gaming \
            -f Containerfile
          podman push ghcr.io/m-fe02/hackpod-os:${{ matrix.image }}-gaming
          podman system prune -a -f

      - name: 'Sign Image'
        env:
          COSIGN_PRIVATE_KEY: ${{ secrets.SIGNING_SECRET }}
        run: |
          cosign sign --yes --key env://COSIGN_PRIVATE_KEY ghcr.io/m-fe02/hackpod-os:${{ matrix.image }}-gaming
